// import { selectAuthenicationDomain } from '../selectors';

describe('selectAuthenicationDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
